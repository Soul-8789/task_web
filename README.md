# task_web
HI, My Name is Baijnath Mahto .

This is my practise repo.
this is made for understand the github commands.

```
git status 
git diff



git init 
git add .
git commit -m " first message"
git branch -M main
git remote add origin `<url>`
git push origin main
```